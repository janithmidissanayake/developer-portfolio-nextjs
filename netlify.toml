[build]
  command = "npm run build"
  # Publish directory is handled automatically by @netlify/plugin-nextjs
  # Do NOT set publish directory here or in Netlify UI

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"

